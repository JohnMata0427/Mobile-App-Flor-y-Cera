describe('Pantalla de catálogo de productos', () => {
	test('Debería mostrar la lista de productos disponibles', () => {});

	test('Debería permitir filtrar productos por categoría', () => {});

	test('Debería mostrar los detalles del producto al hacer clic en un producto', () => {});

	test('Debería permitir agregar productos al carrito desde el catálogo', () => {});
});
