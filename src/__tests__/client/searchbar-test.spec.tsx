describe('Barra de búsqueda', () => {
  test('Debería mostrar resultados relevantes al buscar', () => {});

  test('Debería permitir filtrar resultados por categoría', () => {});

  test('Debería mostrar un mensaje cuando no hay resultados', () => {});
});
